# ============================================
# MiSub Docker 部署环境变量配置模板
# ============================================

# -------------------- 基本配置 --------------------

# 管理员密码 (必需)
# 用于登录管理界面
ADMIN_PASSWORD=your_secure_password_here

# Cookie 加密密钥 (必需)
# 用于加密 session cookie,建议使用 32 位以上随机字符串
# 生成方法: openssl rand -base64 32
COOKIE_SECRET=your_random_secret_key_at_least_32_characters

# -------------------- 服务端口 --------------------

# 后端服务端口
PORT=3200

# 前端开发服务器端口 (仅开发环境)
VITE_PORT=3100

# -------------------- 数据库配置 --------------------

# 数据库文件路径
DB_PATH=./data/misub.db

# 存储类型 (sqlite 为 Docker 部署的唯一选项)
STORAGE_TYPE=sqlite

# -------------------- 应用配置 --------------------

# Node 环境 (production/development)
NODE_ENV=production

# 日志级别 (error/warn/info/debug)
LOG_LEVEL=info

# -------------------- 可选配置 --------------------

# 自定义域名 (可选)
# CUSTOM_DOMAIN=https://misub.yourdomain.com

# 信任代理 (如果在反向代理后面运行,设置为 true)
# TRUST_PROXY=true

# ============================================
# 使用说明:
# 1. 复制此文件为 .env
# 2. 修改 ADMIN_PASSWORD 和 COOKIE_SECRET
# 3. 根据需要调整其他配置
# 4. 不要将 .env 文件提交到 Git
# ============================================
